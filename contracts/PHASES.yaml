phases:
  - id: F00
    name: "create_hello_world"
    next: null
    max_attempts: 3
    agent: "codex"
    prompt: "Create src/generated/hello.py with a function hello() that returns 'Hello, MGK!'"
    required_gates:
      - ROOT_INTEGRITY
      - STATE_INTEGRITY
      - PATCH_SCOPE
      - F00_HELLO_WORLD
    allowed_paths:
      - "src/generated/**"
